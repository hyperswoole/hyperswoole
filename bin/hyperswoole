#!/bin/bash

if [ -z $1 ];then
    cd `dirname $0`
    php hyperswoole.php
elif [ $1 == "reload" ];then
    kill -SIGUSR1 `cat ../log/hyperswoole.pid`
elif [ $1 == "stop" ];then
    kill -15 `cat ../log/hyperswoole.pid`
else
    cd `dirname $0`
    php hyperswoole.php
fi